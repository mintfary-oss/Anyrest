# Copy this file to .env and fill in the values.

# Secret shared between the signaling and relay servers for HMAC authentication.
# MUST be changed before deploying to production!
RELAY_SECRET=change-me-please

# Signal URL for agents (set this when running docker-compose.agent.yml)
ANYREST_SIGNAL_URL=wss://localhost/ws
